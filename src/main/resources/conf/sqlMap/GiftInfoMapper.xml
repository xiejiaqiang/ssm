<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.sales.GiftInfoMapper">
  
  
  <resultMap id="BaseResultMap" type="com.entity.po.sales.TGiftInfo">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="giftNo" jdbcType="VARCHAR" property="giftNo" />
    <result column="giftName" jdbcType="VARCHAR" property="giftName" />
    <result column="giftLink" jdbcType="VARCHAR" property="giftLink" />
    <result column="giftAmount" jdbcType="DECIMAL" property="giftAmount" />
    <result column="channel" jdbcType="VARCHAR" property="channel" />
    <result column="giftImg" jdbcType="VARCHAR" property="giftImg" />
  </resultMap>


  <select id="selectGiftInfo" parameterType="com.entity.po.Example.sales.TGiftInfoExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from t_gift_info g
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="_parameter == null">
      where 1 = 1
    </if>
    <if test="orderByClause != null">
      order by g.${orderByClause}
    </if>
  </select>

  <sql id="Base_Column_List">
    g.id, g.giftNo, g.giftName, g.giftLink, g.giftAmount, g.channel, g.giftImg
  </sql>

  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
    -->
    <where>
      1 = 1
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="and (" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and m.${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and m.${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and m.${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and m.${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>

          </trim>
        </if>
      </foreach>
    </where>
  </sql>
</mapper>